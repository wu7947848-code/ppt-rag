[project]
name = "ppt-rag"
version = "0.1.0"
requires-python = ">=3.10"
description = "PPT Document Structured Retrieval and QA System"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
